# Local dev: API on port 3000, web on 5173 (two processes)
VITE_API_BASE_URL=http://localhost:3000/api
VITE_DEMO_USER_EMAIL=jane.doe@company.com

# Production build options (use one; set when running npm run build):
# - Single app (API serves SPA): VITE_API_BASE_URL=/api  (use .env.production or build arg)
# - Two apps (API and web separate): VITE_API_BASE_URL=https://your-api.azurewebsites.net/api
